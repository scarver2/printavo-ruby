mutation TaskUpdate($id: ID!, $input: TaskInput!) {
  taskUpdate(id: $id, input: $input) {
    id
    body
    dueAt
    completedAt
    assignee {
      id
      firstName
      lastName
    }
  }
}
