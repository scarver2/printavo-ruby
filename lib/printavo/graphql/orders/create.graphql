mutation QuoteCreate($input: QuoteCreateInput!) {
  quoteCreate(input: $input) {
    id
    visualId
    nickname
    total
    createdAt
    updatedAt
    status {
      id
      name
      color
    }
    customer {
      id
      firstName
      lastName
      email
      company
    }
  }
}
