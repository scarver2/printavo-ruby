query Orders($first: Int, $after: String) {
  orders(first: $first, after: $after) {
    nodes {
      id
      visualId
      nickname
      totalPrice
      createdAt
      updatedAt
      status {
        id
        name
        color
      }
      customer {
        id
        firstName
        lastName
        email
        company
      }
    }
    pageInfo {
      hasNextPage
      endCursor
    }
  }
}
