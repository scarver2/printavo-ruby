query Order($id: ID!) {
  order(id: $id) {
    id
    visualId
    nickname
    totalPrice
    createdAt
    updatedAt
    status {
      id
      name
      color
    }
    customer {
      id
      firstName
      lastName
      email
      company
    }
  }
}
