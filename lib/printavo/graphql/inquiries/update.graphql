mutation InquiryUpdate($id: ID!, $input: InquiryInput!) {
  inquiryUpdate(id: $id, input: $input) {
    id
    nickname
    totalPrice
    status {
      id
      name
      color
    }
    customer {
      id
      firstName
      lastName
      email
      company
    }
  }
}
