query Invoices($first: Int, $after: String) {
  invoices(first: $first, after: $after) {
    nodes {
      id
      visualId
      nickname
      total
      amountPaid
      amountOutstanding
      paidInFull
      invoiceAt
      paymentDueAt
      createdAt
      updatedAt
      status {
        id
        name
        color
      }
      contact {
        id
        firstName
        lastName
        email
        phone
      }
    }
    pageInfo {
      hasNextPage
      endCursor
    }
  }
}
