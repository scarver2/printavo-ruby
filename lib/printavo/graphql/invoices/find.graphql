query Invoice($id: ID!) {
  invoice(id: $id) {
    id
    nickname
    total
    amountPaid
    amountOutstanding
    paidInFull
    invoiceAt
    paymentDueAt
    status {
      id
      name
      color
    }
    contact {
      id
      firstName
      lastName
      email
      phone
    }
  }
}
